import { AlmostCorrectDirective } from './almost-correct.directive';

describe('AlmostCorrectDirective', () => {
  it('should create an instance', () => {
    const directive = new AlmostCorrectDirective();
    expect(directive).toBeTruthy();
  });
});
